#include "message.hpp"
#include "error.hpp"

void parsePlayerInfo(const rapidjson::Value & json, PlayerInfo & info)
{
    info = info;
}

int useless_function(int i)
{
    if (i == 4)
        return i;
    else
        return 4;
}
